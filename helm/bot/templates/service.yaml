apiVersion: v1
kind: Service
metadata:
  name: telegram-bot-on-llm
  namespace: {{ .Values.namespace }}
spec:
  type: {{ .Values.service.type }}
  selector:
    app: telegram-bot-on-llm
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.targetPort }}