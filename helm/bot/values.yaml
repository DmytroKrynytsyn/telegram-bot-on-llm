image:
  repository: dkrinitsyn/telegram-bot-on-llm
  pullPolicy: Always
  tag: "20260530142240"

replicaCount: 1

service:
  type: ClusterIP
  port: 80
  targetPort: 8000

env:
  RABBITMQ_URL: "amqp://guest:guest@rabbitmq.rabbitmq.svc.cluster.local/"
  DEFAULT_MODEL: "qwen2.5:32b-instruct-q2_K"
  LLM_TIMEOUT: "180"

namespace: telegram-bot-on-llm